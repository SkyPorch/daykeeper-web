# Daykeeper Web

The official browser client for customer-facing Daykeeper support experiences,
published as `@skyporch/daykeeper-web` after its public-release gates are
approved. It never accepts Daykeeper administrative credentials.

This repository is private and no npm package has been published yet.
