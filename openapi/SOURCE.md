# Contract source

`customer.yaml` was copied from `SkyPorch/daykeeper-openapi` commit
`83f71a5` while its initial stacked pull request was being prepared.

After the first contract release, generation pull requests must copy an
immutable tagged contract and record the tag and full commit SHA here.
